# DTO Pattern

_Generated: 2026-08-06_

## Request DTO

NOT DETECTED — Không tìm thấy class `*Request`, `*Dto`, `@Getter`, `@Builder` patterns.

## Response DTO

NOT DETECTED — Controller hiện trả về `Map<String, Any>` trực tiếp (chưa có typed DTO).

## Filter / Query

NOT DETECTED

## Annotations Used

- `@Service` — `ApiKeyService`
- `@RestController` — `MenuController`
- `@RequestMapping` — `MenuController`
- `@GetMapping` — `MenuController` (2 endpoints)
- `@Component` — `DefaultSessionManagement`

## NOT DETECTED

- Request DTOs (no `*Request` classes)
- Response DTOs (no `*Response` classes)
- Filter objects
- `@Getter`, `@Builder`, `@SuperBuilder` annotations
- Bean Validation annotations (`@NotNull`, `@NotBlank`, etc.)
